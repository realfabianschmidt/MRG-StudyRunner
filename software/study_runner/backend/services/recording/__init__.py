"""Getting signal off the hardware and into a file.

The recording worker's lifecycle, the sensor poll loop, LSL dependency checks,
clock sync, and the quality gates that decide whether a recording is keepable.
"""
