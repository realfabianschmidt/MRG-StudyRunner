"""Run this card using the common extension process protocol."""
from study_runner.plugin_framework.driver_runtime import run_plugin_driver

if __name__ == "__main__":
    raise SystemExit(run_plugin_driver('finish'))
