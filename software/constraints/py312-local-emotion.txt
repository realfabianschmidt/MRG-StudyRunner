# Additional CPython 3.12 constraints for camera_emotion.local_worker.
# Consumed on Windows x64, macOS Apple Silicon, and Linux CI. macOS Intel uses
# remote_worker because TensorFlow publishes no compatible CPython 3.12 wheel.
#
# These pins stabilize the high-risk inference stack but are not a complete,
# hash-locked transitive dependency graph.
deepface==0.0.92
tf-keras==2.20.1
tensorflow==2.20.0
keras==3.12.0
h5py==3.15.1
pandas==2.3.3
