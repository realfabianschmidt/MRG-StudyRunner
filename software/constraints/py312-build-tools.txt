# Build tools for the XDF core: used by the release workflow and by developers
# who build the core from source (--build-core-from-source / -BuildCoreFromSource).
# Normal installs download the prebuilt core and never need CMake.
# This is not a complete or hash-locked build environment.
cmake==3.31.10
